variables:
  - key: BOT_TOKEN
    required: true
  - key: CHANNEL_ID
    required: true
  - key: ADMIN_ID
    required: true

deployments:
  web:
    start: python bot.py
    healthcheckPath: /healthz
